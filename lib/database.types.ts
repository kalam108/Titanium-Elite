/**
 * GENERATED FILE — do not edit by hand.
 *
 * Regenerate with:  npm run db:types
 * Prerequisites:
 *   1. supabase CLI (npx supabase@latest, or npm i -g supabase)
 *   2. a linked project:  npx supabase@latest link  (choose the Phase 2 project)
 *   3. migrations applied: npx supabase@latest db push
 *
 * The generator overwrites this file with the `Database` type inferred from
 * the live database (tables in supabase/migrations/). Until the project is
 * linked this stub stays empty — phase 2 is verified against raw SQL (see
 * supabase/RLS-SMOKE-TEST.md), not this type.
 */
export type Database = Record<string, never>;
